{
  "name": "mozaic.fm",
  "version": "0.0.0",
  "description": "mozaic.fm",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "fmt": "npx @biomejs/biome format --write ./src"

  },
  "devDependencies": {
    "@biomejs/biome": "1.6.2"
  }
}
